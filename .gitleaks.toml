title = "Project gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Allowlist for explicit placeholder values only."
paths = [
  "(^|/)test-data/",
]
regexes = [
  "\\[REDACTED\\]",
]
