@brainwav:registry=https://registry.npmjs.org/
ignore-scripts=true
# Auth should come from user-level ~/.npmrc or CI-injected ~/.npmrc, not this repo.
# This avoids overriding a working npm login with a stale or missing environment token.
